[package]
name = "sourcier-core"
version = "0.1.0"
edition = "2024"

[features]
rt-feedback = []
view = ["dep:memchr"]
default = ["view", "rt-feedback"]
[dependencies]
memchr = { workspace = true, optional = true }
